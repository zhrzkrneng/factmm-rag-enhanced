# Milestone 2.8 Cell 40 -- Resource Discovery Report

Generated by Cell 40, via a fresh, direct read of the vendored official FactMM-RAG repository (`reference/original_repository/FactMM-RAG/`) and `paper/factmm_rag.pdf` this run -- not copied from any prior milestone's summary.

## Major new findings this run

1. **LLaVA fork pinned commit resolved**: `c121f0432da27facab705978f83c4ada465e46fd` (from `install_llava.sh` line 7). This was the single largest `UNKNOWN` carried across Cell 37, Cell 39, and the Milestone 2.8 contract -- now closed.
2. **vicuna base checkpoint identifier resolved**: `lmsys/vicuna-7b-v1.5` (from `src/generator/train_llava.sh` line 6). Previously `UNKNOWN`.
3. **New checkpoint discovered**: `openai/clip-vit-large-patch14-336` -- LLaVA's own generator-stage vision tower, distinct from the retriever's `openai/clip-vit-base-patch32`. Never tracked in this project before.
4. **New checkpoint discovered (identity unresolved)**: an LLaVA multimodal projector checkpoint, referenced only via an environment variable (`PROJECTOR_PATH`) in the README with no specific source named anywhere read this session.
5. **New checkpoint discovered (identity ambiguous)**: an unlabeled Google Drive checkpoint link (`https://drive.google.com/file/d/1qV-atZdKX-PwBSWzEocf5I63vuF9Ri-g/view?usp=sharing`) in the README's RAG section, plausibly a pretrained retriever checkpoint by position/context, but not confirmed.
6. **New internal repo inconsistency discovered**: `accelerate` is pinned to `0.18.0` for the retriever stage (`requirements.txt`) but `0.21.0` for the generator stage (`install_llava.sh` override) -- two different pins across the project's own two required environments.

## Resource inventory summary (10 resources)

- Datasets: 2
- Checkpoints: 7
- Code dependencies: 1
- Mandatory: 9
- Blocking the real baseline run: 7

| Resource | Category | Mandatory/Optional | Blocks real run | Confidence |
|---|---|---|---|---|
| mimic_cxr | dataset | mandatory | True | high |
| chexpert | dataset | mandatory | True | high |
| marvel_warm_start_checkpoint | checkpoint | mandatory | True | high |
| clip_vit_b32_retriever | checkpoint | mandatory | False | high |
| t5_ance | checkpoint | mandatory | False | high |
| llava_base_vicuna | checkpoint | mandatory | True | high (identifier newly confirmed this run, from src/generator/train_llava.sh line 6); medium (everything else about it, still UNKNOWN) |
| llava_fork_haotian_liu | code_dependency | mandatory | True | high (commit hash newly confirmed this run, directly quoted in install_llava.sh line 7) |
| llava_generator_vision_tower_clip_l14 | checkpoint | mandatory (for the generator/RAG-fine-tuning stage only, not the retriever stage) | True | high (identifier newly confirmed this run, from src/generator/train_llava.sh --vision_tower) |
| llava_mm_projector_checkpoint | checkpoint | mandatory | True | low |
| unlabeled_rag_section_checkpoint | checkpoint | optional (presented as an alternative to running the retriever training pipeline yourself, per its position in the README immediately under the '🚀 RAG' heading) | False | low |

## Discrepancies and notable findings (4 recorded)

Grouped by type -- a paper/repo *conflict* (paper says one thing, repo does another) is a different finding from a repo-internal inconsistency or a repo-only detail the paper never addressed; conflating them would misrepresent what was actually found.

### True paper-vs-repository conflicts

- **top_k_positives_per_query**: paper='2 (Implementation Details)' vs repo='3 (gen_topk_pos.sh default)' -- Already known from Milestone 2.6/2.7 audits, reconfirmed unchanged this run.
- **retriever_contrastive_temperature**: paper='fixed tau=0.01 (Appendix A.1)' vs repo='learned logit_scale (train.py)' -- Already known from Milestone 2.6/2.7 audits, reconfirmed unchanged this run.

### Repo-internal inconsistencies (not paper-vs-repo conflicts)

- **accelerate_version_cross_stage**: paper='0.18.0' vs repo='0.21.0' -- NEWLY SURFACED this run: requirements.txt (retriever stage) pins accelerate==0.18.0, but install_llava.sh's generator-stage override pins accelerate==0.21.0 -- this is a repo-internal inconsistency across the two required environments (NOT a paper-vs-repo conflict -- the paper never states an accelerate version at all), not previously recorded at this granularity in this project's own docs.

### Repo-only details the paper is silent on (not conflicts)

- **llava_vision_tower_not_mentioned_in_paper**: paper="UNKNOWN (paper describes generator only as 'LLaVA-1.5 from vicuna-7b-v1.5', no separate vision-tower detail)" vs repo='openai/clip-vit-large-patch14-336' -- NEWLY SURFACED this run: the paper never names a distinct vision tower for the generator stage (so this is NOT a paper-vs-repo conflict -- the paper made no claim to contradict); the official repo pins a specific, different CLIP variant (openai/clip-vit-large-patch14-336) from the one used by the retriever (openai/clip-vit-base-patch32) -- an OFFICIAL_REPOSITORY-only detail the paper is simply silent on, previously untracked anywhere in this project.


## Scope discipline

No dataset or checkpoint was downloaded. No baseline source file or test was modified. Every value not directly confirmed by the paper or the vendored official repository is recorded as `UNKNOWN`, never inferred.
