"""Abstract generator interface.

Responsibility: define the common interface (generate a report given an
image and optional retrieved-evidence context) that every concrete
generator — the real LLaVA-based generator, the mock generator used for
smoke tests, or any innovation generation variant — must implement. No
implementation yet.
"""
