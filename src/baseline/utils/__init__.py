"""Baseline-only helper utilities.

Responsibility: small helpers specific to the baseline reproduction that
don't belong in the project-wide src/common package — e.g. documenting
and resolving the paper-vs-shipped-code hyperparameter discrepancies
(top_k, contrastive temperature) in one place.
"""
