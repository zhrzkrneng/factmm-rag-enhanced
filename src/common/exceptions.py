"""Shared exception types.

Responsibility: define project-wide exception classes (e.g. for config
validation failures, dataset integrity failures, and patient/study
leakage detection) that are raised loudly rather than swallowed, per
CLAUDE.md's rule against silently hiding errors with broad exception
handling. No implementation yet.
"""
