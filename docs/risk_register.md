# Risk Register

Severity: High / Medium / Low. Status: Open / Mitigated / Accepted.

| # | Risk | Area | Severity | Likelihood | Mitigation | Status |
|---|---|---|---|---|---|---|
| 1 | Paper PDF was unavailable; arXiv fetch was blocked by sandbox network policy | Source material | High | N/A — resolved | User uploaded `paper/factmm_rag.pdf` directly via GitHub; merged in and read in full; `docs/paper_analysis.md` rewritten with `PAPER_EXPLICIT` labels | Mitigated (resolved 2026-07-25) |
| 1b | Paper vs. shipped-code discrepancy: paper states **top-2** factual pairs/query (Implementation Details); shipped `gen_topk_pos.sh`/`train.sh` default to **top_k=3** | Reproducibility | Medium | Certain (confirmed by reading both sources) | Expose `top_k` as a config value; run both settings during Milestone 2.3/2.4 and report which better matches paper Table 1 numbers; never silently pick one | Open |
| 1c | Paper vs. shipped-code discrepancy: paper states a **fixed contrastive temperature τ=0.01** (Appendix A.1); `train.py` instead uses a **learned** `logit_scale` parameter initialized from a pretrained CLIP checkpoint (effective starting temperature ≈0.07, not 0.01) | Reproducibility | Medium | Certain (confirmed by reading both sources) | Implement both variants (fixed τ=0.01 vs. learned logit_scale) behind a config flag in Milestone 2.4; document which was actually used to produce our reported numbers | Open |
| 2 | MIMIC-CXR / CheXpert require credentialed access and cannot be auto-downloaded | Data access | High | Certain | Never attempt automated download; document manual acquisition steps; use synthetic fixtures for development/tests | Open (by design — not something to "fix") |
| 3 | Generator training code (`train_llava.sh`, `inference_llava.sh`) exists in the repo but README marks generator training as **not yet released** at clone time. Paper now confirms the target hyperparameters (epochs=1, lr=2e-5, batch=128, vicuna-7b-v1.5, 8x A6000/~4h) but the shipped scripts' actual behavior is still unexecuted/unverified | Reproducibility | High | Medium | Treat generator stage as unverified; run and document actual behavior before trusting it; provide mock generator for pipeline testing meanwhile | Open |
| 4 | MARVEL checkpoint (`OpenMatch/marvel-ance-clueweb`) availability/license/version drift on HuggingFace | Reproducibility | Medium | Low-Medium | Pin exact HF revision hash once downloaded; record in experiment metadata | Open |
| 5 | Two mutually incompatible pinned environments (retriever vs. LLaVA generator) | Engineering | Medium | Certain | Use two separate virtualenvs/conda envs or containers; never merge requirements files | Open |
| 6 | Full-scale factual pair mining (O(n²) over 125,417 reports) and the confirmed 8x-A6000/~4h LLaVA fine-tuning stage are both infeasible on a single Colab/local machine without heavy sub-sampling | Compute | High | Certain at full scale (now paper-confirmed, not a guess) | Restrict full-scale runs to environments matching `docs/compute_requirements.md`; use small/synthetic data for development and correctness testing | Open |
| 7 | Patient/study leakage across splits — official code only filters leakage at RAG-prompt-construction time, not as a standalone split validator | Data integrity | High | Medium (if unverified) | Build an independent split-leakage checker (Milestone 2.1) that runs before any training | Open |
| 8 | RadGraph model checkpoint/version ambiguity (multiple RadGraph variants exist upstream); the paper itself does not state a package/model version | Reproducibility | Medium | Medium | Pin the `radgraph` package version (`0.0.9`, matching official repo) and record it in every experiment | Open |
| 8b | `radgraph==0.0.9`'s vendored ~2022-era AllenNLP code is incompatible with Python 3.12 and modern `transformers`/`huggingface_hub` — 6 distinct issues found by direct execution (Cell 14): a bytecode-introspection bug in a vendored `overrides_` decorator, `transformers.AdamW` removal, an `add_special_tokens` constructor-kwarg conflict, removal of `encode_plus`/`build_inputs_with_special_tokens`, and a missing `"dataset"` field in `preprocess_reports()` that doesn't match the model's actual vocabulary namespace | Reproducibility / Engineering | Medium | Certain (all 6 confirmed via execution) | All 6 fixed via targeted, documented compatibility shims (see `docs/colab_execution_log.md` Cell 14 entry for exact root cause/fix/scientific-impact per shim); 5 of 6 are provably behavior-preserving, 1 (`preprocess_reports`'s dataset field) supplies required data rather than restoring documented behavior and needs re-examination during Milestone 2.3 validation. Shims now live in `src/baseline/radgraph/compat.py`, version-gated (`check_environment()`/`CompatibilityError`), unit-tested (29/29 passing, injected fakes), and independently re-verified end-to-end against the real Colab environment via a Cell 14 rerun (see `docs/colab_execution_log.md`, "Cell 14 — Rerun After Runtime Restart") | Mitigated (moved into version-controlled, tested code; shim 6 still flagged for re-examination in Milestone 2.3) |
| 8c | Official repo's exact `transformers==4.23.1`/`torch==1.13.1` pins are not installable on Python 3.12 (no compatible `tokenizers` wheel for 4.23.1; no `cp312` torch wheel exists for 1.13.1 at all) | Reproducibility | Medium | Certain (confirmed via PyPI metadata + failed install attempts) | Used `transformers==4.57.6` (last pre-"V5" 4.x release, same API generation, has `cp312`-compatible `tokenizers`) with `torch` left at whatever's already installed (`2.11.0+cpu`, never the source of any failure). Documented as a forced, not chosen, deviation. Revisit if ever run on Python ≤3.11 with a working Rust toolchain, where the exact pins might install | Open |
| 9 | Claiming a component "works" based only on code reading, without execution | Process integrity | High | Ongoing risk throughout project | Enforce CLAUDE.md rule: no claim of working code without an observed test/smoke-test run; track in reproduction matrix status column | Mitigated by process (enforced going forward) |
| 10 | Committing large files (weights, embeddings, datasets) accidentally | Repo hygiene | High | Low (if `.gitignore` maintained) | Existing `.gitignore` already excludes datasets, checkpoints, logs, env files; re-verify before every commit involving new binary-ish paths | Mitigated |
| 11 | Reference repository accidentally modified or treated as editable | Process integrity | Medium | Low | Nested `.git` already removed to avoid submodule confusion; directory is documentation-designated read-only; code review should catch accidental edits | Mitigated |
| 12 | Innovation code accidentally mixed into `src/baseline/` | Engineering hygiene | Medium | Low (if directory structure respected) | Enforced by Phase 1 directory layout (`src/baseline/` vs `src/innovation/`); code review before merging any innovation-touching change | Open (structure not yet created) |
| 13 | Paper reports "p-value < 0.05" vs. best baseline (Table 1 caption) but never specifies the test used; bootstrap CIs are not reported at all. Our own significance-testing/CI implementation is therefore a methodological choice, not a reproduction of a specified procedure | Methodology | Low-Medium | Certain (by design) | Document explicitly as our own choice (CI: `PROPOSED_EXTENSION`; significance test: a specific, named test we pick, not "the paper's test") in all reporting; do not present as reproducing the paper's exact statistic | Accepted |
| 14 | LLaVA base weights / fork licensing terms not yet reviewed in full | Legal/compliance | Medium | Low-Medium | Review `haotian-liu/LLaVA` license at the pinned commit before any generator training | Open |
| 15 | F1CheXbert produced two different single-class label predictions (`[0,1,0,...]` vs. index-13 "No Finding") for the identical input sentence, on the identical pinned `f1chexbert==0.0.2`/`transformers==4.57.6` versions, across two separate Cell 14 executions (original v15 run vs. a later rerun after a Colab runtime restart) — see `docs/colab_execution_log.md` for both outputs | Reproducibility / Metric validity | Medium | Confirmed (directly observed both outputs) | Not yet investigated; root cause is unconfirmed. Possible explanations include, but are not limited to: (a) an uncontrolled source of nondeterminism in `F1CheXbert.get_label()`'s inference path that this project has not pinned/seeded; (b) a preprocessing- or environment-sensitive code path (e.g. a differently-resolved tokenizer revision between the two runs) producing a materially different input to the classifier despite identical source text. These are hypotheses only, not findings. Must be investigated before Milestone 2.6 (evaluation), where run-to-run F1CheXbert score stability is required for any reported number to be trustworthy. Out of scope for Milestone 2.2. The Cell 16 real annotation-pipeline smoke test (`docs/colab_execution_log.md`) did not investigate this risk and does not change its status — still open, unresolved, and not blocking Milestone 2.2 | Open |
